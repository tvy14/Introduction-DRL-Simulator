import { defineConfig } from 'vite';

// Use './' so asset paths are relative — works for GitHub Pages
// regardless of repo subdirectory (https://le-liang.github.io/wcmlbook/).
export default defineConfig({
  base: './',
});
